import { Component } from 'react'
import { Layout } from 'antd'

const { Footer } = Layout

class FSFooter extends Component {
    render() {
        return (
            <Footer style={{ textAlign: 'center' }}>PyPan @2021</Footer>
        )
    }
}

export default FSFooter
